# Purpose: Provides a local MongoDB service for running BytesAndBeyond development APIs.
services:
  mongo:
    image: mongo:7
    container_name: bytesandbeyond-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
