{
  "name": "bytesandbeyond",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace orchestration for the BytesAndBeyond full-stack application.",
  "workspaces": [
    "frontend",
    "backend",
    "database"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "seed": "npm run seed --workspace database"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
